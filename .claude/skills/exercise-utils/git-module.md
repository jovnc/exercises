# git-module.md

## Overview
Wrapper functions for Git CLI commands.

**When to use**: For all Git operations - never call `git` directly via subprocess or shell commands.

## Functions
**Core operations**: init, add, commit, checkout, merge, push, tag, remote management

Full API: [exercise_utils/git.py](../../../exercise_utils/git.py)
